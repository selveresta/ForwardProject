class Profile:

    def __init__(self, app_id, app_hash, phone, session_name):
        self.app_id = app_id
        self.app_hash = app_hash
        self.phone = phone
        self.session_name = session_name
